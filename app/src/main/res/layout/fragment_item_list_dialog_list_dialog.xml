<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="photo"
            type="xyz.izadi.oishiiwallpapers.data.api.UnsplashPhoto" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/navigation_view_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_rounded_top"
        app:behavior_hideable="false"
        app:layout_behavior="@string/bottom_sheet_behavior">

        <TextView
            android:id="@+id/textView"
            style="@style/TextAppearance.MaterialComponents.Headline5"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="8dp"
            android:text="@string/photo_information"
            android:textColor="?colorOnSurface"
            android:textStyle="bold"
            app:layout_constraintStart_toEndOf="@+id/imageView"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/textView2"
            style="@style/TextAppearance.MaterialComponents.Subtitle1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:alpha="0.87"
            android:text="@string/subtitle_camera_info"
            android:textColor="?colorOnSurface"
            app:layout_constraintStart_toStartOf="@+id/textView"
            app:layout_constraintTop_toBottomOf="@+id/textView" />

        <View
            android:id="@+id/navigation_divider"
            style="@style/Divider"
            android:layout_marginTop="12dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/textView2" />

        <ImageView
            android:id="@+id/imageView"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginStart="16dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="8dp"
            android:alpha="0.87"
            android:background="@drawable/ic_info"
            android:backgroundTint="@color/design_default_color_secondary"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@+id/textView" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="256dp"
            android:orientation="vertical"
            android:padding="16dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@id/navigation_divider">

            <TextView
                style="@style/PhotoInfoLabel"
                visibleGone="@{photo.description != null}"
                android:text="@string/dv_description" />

            <TextView
                style="@style/PhotoInfoItem"
                visibleGone="@{photo.description != null}"
                android:text="@{photo.description}"
                tools:text="This is a sample description to see how it would look" />

            <TextView
                style="@style/PhotoInfoLabel"
                visibleGone="@{photo.width != 0}"
                android:text="@string/dv_size" />

            <TextView
                style="@style/PhotoInfoItem"
                visibleGone="@{photo.width != 0}"
                android:text="@{photo.width + ` x ` + photo.height}"
                tools:text="1080 x 1290" />
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
